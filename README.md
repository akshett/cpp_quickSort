This is a basic C++ implementation of the quick sort which is a recursive algorithm. The function is written with a generic template and can be used for any comparable type (types that compare using '<', '>').
